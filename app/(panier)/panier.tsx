import { View, Text } from 'react-native'
export default function Panier() {
  return <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Panier — à venir</Text>
  </View>
}
